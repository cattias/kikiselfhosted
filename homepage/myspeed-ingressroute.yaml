apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: myspeed-router
  namespace: home
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`myspeed.attias.io`)
      kind: Rule
      services:
        - name: myspeed
          port: 5216
    #   priority: 10
    #   middlewares:
    #     - name: authentik
    #       namespace: traefik
    # - kind: Rule
    #   match: "Host(`myspeed.attias.io`) && PathPrefix(`/outpost.goauthentik.io/`)"
    #   priority: 15
    #   services:
    #       - kind: Service
    #         name: ak-outpost-authentik-embedded-outpost
    #         namespace: authentik
    #         port: 9000
  tls:
    certResolver: le