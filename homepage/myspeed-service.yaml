apiVersion: v1
kind: Service
metadata:
  name: myspeed
  namespace: home
  labels:
    app.kubernetes.io/name: myspeed
spec:
  type: ClusterIP
  ports:
    - name: "5216"
      port: 5216
      targetPort: 5216
  selector:
    app.kubernetes.io/name: myspeed