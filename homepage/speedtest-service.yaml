apiVersion: v1
kind: Service
metadata:
  name: speedtest
  namespace: home
  labels:
    app.kubernetes.io/name: speedtest
spec:
  type: ClusterIP
  ports:
    - name: "8765"
      port: 8765
      targetPort: 8765
  selector:
    app.kubernetes.io/name: speedtest