apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homepage-router
  namespace: home
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`attias.io`)
      kind: Rule
      services:
        - name: homepage
          port: 80
  tls:
    certResolver: le