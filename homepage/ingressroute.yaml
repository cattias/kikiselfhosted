apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homepage-router
  namespace: home
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`attias.io`) || Host(`www.kiki.home`)
      kind: Rule
      services:
        - name: homepage
          port: 80