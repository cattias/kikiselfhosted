apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: videospvc
  namespace: plex
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  selector:
    matchLabels:
      type: nfs-videos
