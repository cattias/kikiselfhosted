apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: videos-pvc
  namespace: plex
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  selector:
    matchLabels:
      type: nfs-videos
