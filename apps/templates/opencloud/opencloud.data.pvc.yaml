apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencloud-data-pvc
  namespace: documents
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 100Gi