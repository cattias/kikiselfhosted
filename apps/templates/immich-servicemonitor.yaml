apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: immich-api-monitor
  namespace: photos
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: immich-api
  endpoints:
  - port: http-metrics
    path: /metrics
