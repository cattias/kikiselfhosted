apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: mealie-daily-backup
  namespace: mealie
spec:
  immediate: true
  schedule: "0 0 0 * * *"  # At midnight every day
  backupOwnerReference: self
  cluster:
    name: mealie-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io