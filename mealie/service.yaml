apiVersion: v1
kind: Service
metadata:
  name: mealie
  namespace: mealie
  labels:
    app.kubernetes.io/name: mealie
spec:
  ports:
    - name: "9925"
      port: 9000
      targetPort: 9000
  selector:
    app.kubernetes.io/name: mealie