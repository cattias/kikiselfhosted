apiVersion: v1
kind: Service
metadata:
  name: mealie
  namespace: mealie
  labels:
    app.kubernetes.io/name: mealie
spec:
  type: NodePort
  ports:
    - name: "9925"
      port: 9925
      targetPort: 9000
  selector:
    app.kubernetes.io/name: mealie