apiVersion: v1
kind: PersistentVolume
metadata:
  name: mealie-backup-pv
spec:
  capacity:
    storage: 10Ti
  accessModes: [ReadWriteMany]
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: nfs.csi.k8s.io
    volumeHandle: mealie-nas-backup-share
    volumeAttributes:
      server: 172.16.10.2
      share: /volume1/backup
  mountOptions:
    # - nfsvers=4.1
    - hard
    - noac
    - timeo=600
    - retrans=2
    - noatime
    - tcp
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-backup-pvc
  namespace: mealie
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 10Ti
  volumeName: mealie-backup-pv
  storageClassName: ""