apiVersion: v1
kind: Service
metadata:
  name: opencloud
  namespace: documents
  labels:
    app.kubernetes.io/name: opencloud
spec:
  type: ClusterIP
  ports:
    - name: "9140"
      port: 9140
      targetPort: 9140
  selector:
    app.kubernetes.io/name: opencloud