apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencloud-onlyoffice-pvc
  namespace: documents
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 100Gi