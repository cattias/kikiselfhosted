apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: paperless-db
  namespace: documents
spec:
  instances: 2

  primaryUpdateStrategy: unsupervised

  storage:
    size: 50Gi
    storageClass: ceph-block

  walStorage:
    enabled: true
    size: 1Gi
    storageClass: ceph-block

  monitoring:
    enabled: true