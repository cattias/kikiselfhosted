apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: nextcloud-db
  namespace: nextcloud
spec:
  instances: 2

  primaryUpdateStrategy: unsupervised

  storage:
    size: 1Gi
    storageClass: ceph-block
