apiVersion: v1
kind: Service
metadata:
  name: lidarr
  namespace: arr
  labels:
    app.kubernetes.io/name: lidarr
spec:
  type: NodePort
  ports:
    - name: "8686"
      port: 8686
      targetPort: 8686
  selector:
    app.kubernetes.io/name: lidarr