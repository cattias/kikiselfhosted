apiVersion: v1
kind: Service
metadata:
  name: lidarr
  namespace: arr
  labels:
    app.kubernetes.io/name: lidarr
spec:
  type: LoadBalancer
  ports:
    - name: "8686"
      port: 80
      targetPort: 8686
  selector:
    app.kubernetes.io/name: lidarr