apiVersion: v1
kind: Service
metadata:
  name: transmission
  namespace: arr
  labels:
    app.kubernetes.io/name: transmission
spec:
  type: LoadBalancer
  ports:
    - name: "9091"
      port: 9091
      targetPort: 9091
  selector:
    app.kubernetes.io/name: transmission