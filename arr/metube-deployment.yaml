apiVersion: apps/v1
kind: Deployment
metadata:
  name: metube
  namespace: arr
  labels:
    app.kubernetes.io/name: metube
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: metube
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: metube
    spec:
      containers:
        - env:
            - name: AUDIO_DOWNLOAD_DIR
              value: /audio-downloads
            - name: GID
              value: "10"
            - name: UID
              value: "1000"
            - name: TZ
              value: UTC
          name: metube
          image: alexta69/metube:2025.12.26
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              protocol: TCP
          volumeMounts:
            - mountPath: /downloads
              name: nas-metube-volume
            - mountPath: /audio-downloads
              name: nas-music-volume
      restartPolicy: Always
      volumes:
        - name: nas-metube-volume
          hostPath:
            path: "/mnt/ugreen-media/metube/downloads"
            type: DirectoryOrCreate
        - name: nas-music-volume
          hostPath:
            path: "/mnt/ugreen-media/soulsolid/downloads"
            type: DirectoryOrCreate
