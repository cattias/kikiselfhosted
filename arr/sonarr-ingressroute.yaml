apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-router
  namespace: arr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr.attias.io`)
      kind: Rule
      services:
        - name: sonarr
          port: 8989
  tls:
    certResolver: le