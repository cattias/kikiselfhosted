apiVersion: v1
kind: Service
metadata:
  name: beets
  namespace: arr
  labels:
    app.kubernetes.io/name: beets
spec:
  type: NodePort
  ports:
    - name: "8337"
      port: 8337
      targetPort: 8337
  selector:
    app.kubernetes.io/name: beets