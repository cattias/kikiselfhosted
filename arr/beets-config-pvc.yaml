apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: beets
  name: beets-config-pvc
  namespace: arr
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block