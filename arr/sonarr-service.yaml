apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: arr
  labels:
    app.kubernetes.io/name: sonarr
spec:
  type: LoadBalancer
  ports:
    - name: "8989"
      port: 80
      targetPort: 8989
  selector:
    app.kubernetes.io/name: sonarr