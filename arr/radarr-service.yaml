apiVersion: v1
kind: Service
metadata:
  name: radarr
  namespace: arr
  labels:
    app.kubernetes.io/name: radarr
spec:
  type: LoadBalancer
  ports:
    - name: "7878"
      port: 80
      targetPort: 7878
  selector:
    app.kubernetes.io/name: radarr