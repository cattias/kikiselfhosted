apiVersion: v1
kind: Service
metadata:
  name: overseerr
  namespace: arr
  labels:
    app.kubernetes.io/name: overseerr
spec:
  type: ClusterIP
  ports:
    - name: "5055"
      port: 5055
      targetPort: 5055
  selector:
    app.kubernetes.io/name: overseerr