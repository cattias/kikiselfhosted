apiVersion: v1
kind: Service
metadata:
  name: seerr
  namespace: arr
  labels:
    app.kubernetes.io/name: seerr
spec:
  type: ClusterIP
  ports:
    - name: "5055"
      port: 5055
      targetPort: 5055
  selector:
    app.kubernetes.io/name: seerr