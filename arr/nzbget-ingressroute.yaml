apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nzbget-router
  namespace: arr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`nzbget.attias.io`)
      kind: Rule
      services:
        - name: nzbget
          port: 6789
      priority: 10
    #   middlewares:
    #     - name: authentik
    #       namespace: traefik
    # - kind: Rule
    #   match: "Host(`nzbget.attias.io`) && PathPrefix(`/outpost.goauthentik.io/`)"
    #   priority: 15
    #   services:
    #       - kind: Service
    #         name: ak-outpost-authentik-embedded-outpost
    #         namespace: authentik
    #         port: 9000
  tls:
    certResolver: le