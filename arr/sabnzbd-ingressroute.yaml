apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sabnzbd-router
  namespace: arr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sabnzbd.attias.io`)
      kind: Rule
      services:
        - name: sabnzbd
          port: 8080
  tls:
    certResolver: le