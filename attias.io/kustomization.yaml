apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - attias.io.deployment.yaml

configMapGenerator:
  - name: static-content
    files:
      - static/index.html
      - static/favicon.ico
