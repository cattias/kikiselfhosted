apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: paperless-db
  namespace: paperless
spec:
  instances: 2

  primaryUpdateStrategy: unsupervised

  storage:
    size: 1Gi
    storageClass: ceph-block
